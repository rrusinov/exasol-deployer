Add to the setup-exasol-ansible.yml playbook at the end followng steps:
verify the checksums of the downloaded c4 and db pacakges
add to .ssh/config hostname cos equal to "ssh -p 20002 root@localhost"
verify if the generated c4 configuration is correct:  cd /home/{{ exa_user }}/exasol-release && ./c4 host diag -i config
start the database deployment: cd /home/{{ exa_user }}/exasol-release && ./c4 host play -i config
wait with timeout of 5 minutes until the database boots: c4 wait --stage d





Add needed permissions options in deploy which runs over initilized deploy and checks which cloud permissions are needed to deploy/destroy the deployment.
Use this tool, run it locally in docker or podman (fallback) if needed https://github.com/JamesWoolfenden/pike?tab=readme-ov-file and print json output like this:

REQUIRED_PERMISSIONS = {
    // EC2 Permissions - Core compute and networking
    ec2: [
        'ec2:RunInstances',
        'ec2:DescribeInstanceAttribute',
        ...
    ],
    // VPC and Networking
    vpc: [
        'ec2:CreateVpc',
        'ec2:DeleteVpc',
        ...
    ],
    ...
}
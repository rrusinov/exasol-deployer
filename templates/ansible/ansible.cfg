[defaults]
# Fix deprecation warning: explicitly set inject_facts_as_vars to False
# This prevents top-level facts from being auto-injected as variables
inject_facts_as_vars = False

# Standard settings
host_key_checking = False
timeout = 30
